# Wizard — Python dependencies
# Install: pip install -r requirements.txt
# Requires Python 3.10+

# ── Installation Instructions ────────────────────────────────────────────────
# 
# 1. Base installation (all platforms):
#    pip install -r requirements.txt
#
# 2. GPU acceleration (OPTIONAL - platform-specific, not required):
#    
#    Windows/Linux with NVIDIA GPU:
#      pip install torch>=2.5.0 torchvision>=0.20.0 onnxruntime-gpu --index-url https://download.pytorch.org/whl/cu121
#    
#    Mac Apple Silicon (M1/M2/M3):
#      pip install torch>=2.5.0 torchvision>=0.20.0
#    
#    CPU only:
#      Nothing needed - already included in requirements.txt!
#
# The project auto-detects GPU availability and uses it if present.
# GPU packages are NOT in requirements.txt to maintain platform independence.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core web server ──────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
python-dotenv>=1.0.0

# ── LLM providers & Orchestration ───────────────────────────────────────────
anthropic>=0.40.0
# openai>=1.50.0          # uncomment if using OpenAI provider
google-generativeai>=0.3.0  # for Gemini provider

# LangGraph orchestration framework
langgraph>=0.2.0           # Agent orchestration with state management (Send API from 0.2.0+)
langgraph-checkpoint-sqlite>=1.0.0  # SQLite checkpointing for conversation persistence
aiosqlite>=0.19.0          # Async SQLite support for AsyncSqliteSaver
langchain-core>=0.1.50     # Core abstractions for LLMs and tools
langchain-anthropic>=0.1.0  # Anthropic Claude LangChain integration
langchain-google-genai>=0.0.5  # Google Gemini LangChain integration
langsmith>=0.1.0           # Observability and tracing (optional but recommended)

# ── Transcription & ML (CPU-compatible, works on all platforms) ──────────────
# Base packages - platform-independent:
optimum[onnxruntime]>=1.14.0  # ONNX Runtime (CPU version) - cross-platform
transformers>=4.46.0  # Updated for PyTorch 2.6+ compatibility and safetensors support

# NOTE: For GPU acceleration, see installation instructions at top of file.
# ONNX Runtime automatically uses: CUDA (Windows/Linux) → CoreML (Mac) → CPU

# ── Embeddings ───────────────────────────────────────────────────────────────
sentence-transformers>=3.0.0
# Downloads ~80 MB model (all-MiniLM-L6-v2) on first use

# ── Vector database ──────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Utilities ────────────────────────────────────────────────────────────────
psutil>=5.9.0              # RAM detection for auto model selection

# FFmpeg is a system dependency — install separately:
#   Windows: winget install Gyan.FFmpeg   (or scoop install ffmpeg)
#   Mac:     brew install ffmpeg
#   Linux:   apt install ffmpeg

# ── Visual & Audio analysis ──────────────────────────────────────────────────
Pillow>=10.0.0             # Image processing for ColorAgent
numpy>=1.24.0              # Required by librosa and image analysis
librosa>=0.10.0            # AudioAgent — audio feature extraction
soundfile>=0.12.0          # Required by librosa for audio I/O
numba>=0.57.0              # librosa performance optimization
